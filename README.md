# Z.ai Limits Homebrew Tap

## Install

```bash
brew tap DmitryReztsov/tap
brew install --cask zailimits
```

## Update

After a new release, update the `sha256` and `version` in `Casks/zailimits.rb` to match the new release.
